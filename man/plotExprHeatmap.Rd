% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotExprHeatmap.R
\name{plotExprHeatmap}
\alias{plotExprHeatmap}
\title{Expression Heatmap}
\usage{
plotExprHeatmap(
  x,
  features = NULL,
  by = c("sample_id", "cluster_id", "both"),
  k = "meta20",
  m = NULL,
  assay = "exprs",
  fun = c("median", "mean", "sum"),
  scale = c("first", "last", "never"),
  q = 0.01,
  row_anno = TRUE,
  col_anno = TRUE,
  row_clust = TRUE,
  col_clust = TRUE,
  row_dend = TRUE,
  col_dend = TRUE,
  bars = FALSE,
  perc = FALSE,
  bin_anno = FALSE,
  hm_pal = rev(RColorBrewer::brewer.pal(11, "RdYlBu")),
  k_pal = .marmot_cluster_cols,
  m_pal = k_pal,
  distance = c("euclidean", "maximum", "manhattan", "canberra", "binary", "minkowski"),
  linkage = c("average", "ward.D", "single", "complete", "mcquitty", "median",
    "centroid", "ward.D2")
)
}
\arguments{
\item{x}{A SingleCellExperiment object}

\item{features}{Character vector of features to include. If NULL, uses all features.}

\item{by}{Character specifying grouping: "sample_id", "cluster_id", or "both"}

\item{k}{Character specifying clustering to use (default: "meta20")}

\item{m}{Character specifying metaclustering (optional)}

\item{assay}{Character specifying which assay to use (default: "exprs")}

\item{fun}{Character specifying aggregation function: "median", "mean", or "sum"}

\item{scale}{Character specifying when to scale: "first", "last", or "never"}

\item{q}{Numeric quantile for scaling (default: 0.01)}

\item{row_anno}{Logical or character vector for row annotations (default: TRUE)}

\item{col_anno}{Logical or character vector for column annotations (default: TRUE)}

\item{row_clust}{Logical for row clustering (default: TRUE)}

\item{col_clust}{Logical for column clustering (default: TRUE)}

\item{row_dend}{Logical for row dendrogram display (default: TRUE)}

\item{col_dend}{Logical for column dendrogram display (default: TRUE)}

\item{bars}{Logical for frequency bars (default: FALSE)}

\item{perc}{Logical for percentage display (default: FALSE)}

\item{bin_anno}{Logical for binary annotation display (default: FALSE)}

\item{hm_pal}{Colour palette for heatmap (default: rev(RColorBrewer::brewer.pal(11, "RdYlBu")))}

\item{k_pal}{Colour palette for cluster annotations (default: .marmot_cluster_cols)}

\item{m_pal}{Colour palette for metacluster annotations (default: k_pal)}

\item{distance}{Character specifying distance metric for clustering}

\item{linkage}{Character specifying linkage method for clustering}
}
\value{
A ComplexHeatmap object
}
\description{
Create a heatmap of expression values with customizable clustering and annotations.
This function masks the CATALYST::plotExprHeatmap function but removes the factor relevelling
}
\note{
This function masks CATALYST::plotExprHeatmap. Load this package after CATALYST 
      or use \code{conflicted} package to manage function conflicts.
}
\author{
Helena Crowell (original CATALYST implementation)
}
